-- Configuration moved to blink-cmp.lua as LazyVim now uses blink.cmp by default
-- This file is kept for compatibility but nvim-cmp is disabled
return {
  {
    "hrsh7th/nvim-cmp",
    enabled = false, -- Disabled in favor of blink.cmp
  },
}
